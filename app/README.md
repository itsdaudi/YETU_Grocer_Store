cat > README.md << 'EOF'
# Yetu Grocer Store — Backend

Flask REST API for the Yetu Grocer Store project.

## Setup
1. `pipenv install`
2. `pipenv shell`
3. `python run.py`

API runs at `http://localhost:5000`
EOF